import { FileText } from 'lucide-react'
import PagePlaceholder from '../components/layout/PagePlaceholder'

export default function RecordsPage() {
  return (
    <PagePlaceholder
      icon={FileText}
      title="Records"
      description="Licence pipeline and student records."
    />
  )
}
